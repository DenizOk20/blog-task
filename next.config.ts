import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
      domains: ['images.unsplash.com','miro.medium.com','plus.unsplash.com'],
    },
};

export default nextConfig;
